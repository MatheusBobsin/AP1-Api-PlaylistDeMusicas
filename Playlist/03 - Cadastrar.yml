info:
  name: 03 - Cadastrar
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/musicas"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
